{
  "name": "ttp-challenge-leaderboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "update:leaderboard": "node scripts/update-leaderboard.mjs"
  }
}
